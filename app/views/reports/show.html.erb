<p>
  <strong><%= Book.human_attribute_name(:title) %>:</strong>
  <%= @report.title %>
</p>

<p>
  <strong><%= Book.human_attribute_name(:body) %>:</strong>
  <%= @report.body %>
</p>

<% if current_user.id == @report.user_id %>
  <%= link_to 'Edit', edit_report_path(@report) %> |
<% end %>
<%= link_to 'Back', user_path(@report.user_id) %>
